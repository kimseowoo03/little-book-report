import classes from "../../styles/Loading.module.scss";

const Loading = () => {
  return (
    <div className={classes.loading}>
      <span></span>
      <span></span>
      <span></span>
    </div>
  );
};

export default Loading;
